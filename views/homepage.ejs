<!DOCTYPE html>
<head>
    <%- include('head'); %>
</head>
<body>
<header>
    <%- include('header'); %>
</header>
<div class ="wrapper">
    <% for (let i = 0; i < posts.length; i += 1) { %>
        <div class = "home-posts">
            <div class="article">
                <h2 class="article-header">Post Name:<%= posts[i].post_name %></h2>
                <div class="article-autor">Nickname<%= posts[i].post_author %></div>
                <li class="article-post-time">0000.00.00 00:00<%= posts[i].publication_date %></li>
                <p class="article-text">...POST TEXT...<%= posts[i].post_content %></p>
                <div class="all-posts">
                    <% for (var j = 0; j < post[i].post_tags.length;j += 1) { %>
                    <p class="all-article-tag"><%= posts[i].post_tags[j] %>
                        <% } %>
                </div>
            </div>
        </div>
    <% } %>
</div>
<footer>
    <%- include('footer'); %>
</footer>
</body>