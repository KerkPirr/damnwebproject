<!DOCTYPE html>
<head>
    <%- include('head'); %>
</head>
<body>
<div class="wrapper">
    <header>
        <%- include('header'); %>
    </header>

    <h2> All posts </h2>

    <% for (var i = 0; i < posts.length; i += 1) { %>
        <div class="container">
            <div class="article">
                <h3 class="article-header"><%= posts[i].post_name %></h3>
                <div class="article-autor"><%= posts[i].post_author %></div>
                <li class="article-post-time"><%= posts[i].pub_date.toLocaleDateString('en-GB').replaceAll('/','.') %></li>
                <div class = "img_comment">
                <img src="<%= posts[i].post_pict%>" alt="Здесь должна была быть картинка">
                </div>
                <p class="article-text"><%= posts[i].post_content %></p>
            </div>
        </div>
    <% } %>

    <footer>
        <%- include('footer'); %>
    </footer>
</div>
</body>